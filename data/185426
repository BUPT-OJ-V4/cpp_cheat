/*
USER_ID: bupt#100136111311073
PROBLEM: 998
SUBMISSION_TIME: 2016-03-25 14:29:46
*/
#include <cstdio>
#include <cstring>
#include <iostream>

using namespace std;

int main()
{
    int t, n, m;
    cin >> t;
    while (t--) {
        int ans = 0;
        cin >> n >> m;
        for (int i = 1; i <= n; ++i){
            if (m % i == 0 && (m / i) >= 1 && (m / i) <= n) {
               ans++;
            }
        }
        printf("%d\n", ans);
    }
    return 0;    
}
