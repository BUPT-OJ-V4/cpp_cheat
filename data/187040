/*
USER_ID: bupt#100136111311340
PROBLEM: 998
SUBMISSION_TIME: 2016-03-25 15:35:55
*/
#include<stdio.h>

int main()
{
    int T;    //T组 ，每组N[i]个数 
    int n[20],m[20];   //n为矩阵大小，m为查找的数 
	int i,j,k;
	int count;
	int mix;
	scanf("%d",&T);
	for(i=0;i<T;i++)
	{
		scanf("%d",&n[i]);
		scanf("%d",&m[i]);
	}
 	for(i=0;i<T;i++)
 	{
	 	count=0;
	 	if(n[i]>m[i])
	 	mix=m[i];
	 	else
	 	mix=n[i];
	    for(j=1;j<=mix;j++)
	 	{
	 	   if(m[i]%j==0&&m[i]/j!=j&&(m[i]/j)<n[i])	
	 	   count++;
	 	   else if(m[i]==(j*j))
	 	   count++;
	 	}
	 	printf("%d\n",count);
 	}
}
