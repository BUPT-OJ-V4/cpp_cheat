/*
USER_ID: bupt#100136111311042
PROBLEM: 998
SUBMISSION_TIME: 2016-03-25 14:52:32
*/
#include<stdio.h>
int main(){
    int T = 0, res[20] = {0};
    scanf("%d",&T);
    for(int i = 0; i < T; i++){
            long int n,m;
            int count = 0;
            scanf("%ld %ld",&n,&m);
            for(int j = 1; j <= n; j++){
                    if(m%j == 0 && m/j <= n)count++;
                    }
            res[i] = count;
            }
    for(int i = 0; i < T; i++){
            printf("%d\n",res[i]);
            }
    return 0;
}
