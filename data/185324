/*
USER_ID: bupt#100136009030083
PROBLEM: 998
SUBMISSION_TIME: 2016-03-25 14:25:36
*/
#include<stdio.h>
#include<string>
#include<iostream>
#include<vector>
#include<map>
#include<string.h>
#include<cmath>
#include<algorithm>
using namespace std;
char seq[1001];
int main()
{
    int t;
    scanf("%d",&t);
    while(t--)
    {
        int n,m;
        scanf("%d %d",&n,&m);
        //int max = sqrt((double)m);
        int count = 0;
        for(int i=1;i<=n ;i++)
        {
            if(m%i==0)
            {
                if(i==m/i)
                    count++;
                else
                {
                    if(m/i<=n)
                        count++;
                }
            }
        }
        printf("%d\n",count);
    }
    return 0;
}
