/*
USER_ID: bupt#100136111311209
PROBLEM: 998
SUBMISSION_TIME: 2016-03-25 15:58:22
*/
#include<stdio.h>

int main()
{
    int num;
    scanf("%d", &num);
    for(int i=0; i<num; i++)
    {
            int n;
            int m;
            int sum=0;
            scanf("%d%d", &n, &m);
            for(int i=1; i<=n; i++)
            {
                    if(m%i == 0)
                    {
                           if(m/i <= n) sum++;
                    }
            }
            printf("%d\n", sum);
    }
    return 0;
}
