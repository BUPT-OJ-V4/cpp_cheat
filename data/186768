#include <stdio.h>
#include <math.h>
int main(){
	int T,i;
	long n,m;
	void test(long n,long m);
	scanf("%d",&T);
	for(i=0;i<T;i++){
		scanf("%ld %ld",&n,&m);
		test(n,m);
	}
	return 0;
}

void test(long n,long m){
	//long long a[n];
	long i,j,count=0,border;
	border=(m>n)?n:m;
	for(i=1;i<=border;i++){
		if((m%i==0)&&((m/i)<=border)){
			count++;
		}
	}
	
	printf("%ld\n",count);
}